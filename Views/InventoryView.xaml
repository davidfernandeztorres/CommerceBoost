<UserControl x:Class="CommerceBoost.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gestión de Inventario" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Foreground="#111827"/>

        <!-- Scanner Simulation for Inventory -->
        <Border Grid.Row="1" Background="#F3F4F6" Padding="15" CornerRadius="8" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Simulación de Entrada de Stock" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal">
                    <Button Content="📦 ESCANEAR NUEVO PRODUCTO" 
                            Command="{Binding ScanNewProductCommand}"
                            Background="#3B82F6" 
                            Foreground="White"
                            Padding="15,10"
                            FontWeight="SemiBold"
                            BorderThickness="0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Inventory Grid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Products}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  Background="White"
                  BorderBrush="#E5E7EB"
                  BorderThickness="1"
                  RowHeight="45"
                  FontSize="14">
            <DataGrid.Columns>
                <DataGridTextColumn Header="CÓDIGO" Binding="{Binding Codigo}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="NOMBRE" Binding="{Binding Nombre}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="PRECIO" Binding="{Binding Precio, StringFormat=C2}" Width="100"/>
                <DataGridTextColumn Header="STOCK" Binding="{Binding Stock}" Width="100"/>
            </DataGrid.Columns>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F9FAFB"/>
                    <Setter Property="Foreground" Value="#6B7280"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="BorderBrush" Value="#E5E7EB"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>
    </Grid>
</UserControl>
